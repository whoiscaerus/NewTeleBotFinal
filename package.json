{
  "name": "trading-signal-platform",
  "version": "1.0.0",
  "description": "Trading signal platform with AI-powered strategies",
  "workspaces": [
    "frontend/web",
    "frontend/packages/*",
    "frontend/miniapp"
  ],
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "test:ci": "jest --coverage --ci --maxWorkers=2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.1",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2"
  }
}
